<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="edu.kh.project.board.model.mapper.CommentMapper">


	<!-- 댓글 목록 조회 -->
	<!-- 댓글 목록 조회 -->
	<select id="commentList">
		SELECT LEVEL, C.* FROM
			(SELECT 
					COMMENT_NO, 
					COMMENT_TEXT,
				  TO_CHAR(COMMENT_REGI_DATE, 'YYYY"년" MM"월" DD"일" HH24"시" MI"분" SS"초"') COMMENT_REGI_DATE,
				  BOARD_NO, 
				  MEMBER_NO, 
				  MEMBER_NICKNAME, 
				  PROFILE_IMG, 
				  PARENT_COMMENT_NO, 
				  COMMENT_DEL_FL
			FROM "TB_COMMENT"
			JOIN "TB_MEMBER" USING(MEMBER_NO)
			WHERE BOARD_NO = #{boardNo}) C
		WHERE COMMENT_DEL_FL = 'N'
		OR 0 != (SELECT 
						COUNT(*) 
				FROM "TB_COMMENT" SUB
				WHERE SUB.PARENT_COMMENT_NO = C.COMMENT_NO
				AND COMMENT_DEL_FL = 'N')
		START WITH PARENT_COMMENT_NO IS NULL
		CONNECT BY PRIOR COMMENT_NO = PARENT_COMMENT_NO
		ORDER SIBLINGS BY COMMENT_NO
	</select>
	

	<!-- 댓글 수정 -->
	<update id="commentUpdate">
		UPDATE "TB_COMMENT"
		SET
			COMMENT_TEXT = #{commentText}
		WHERE COMMENT_NO = #{commentNo}
	</update>
	
	
	<!-- 댓글 삭제 -->
	<delete id="commentDelete">
		UPDATE "TB_COMMENT"
		SET
			COMMENT_DEL_FL = 'Y'
		WHERE COMMENT_NO = #{commentNo}
	</delete>
	
	
	<!-- 답글 등록 -->
	<insert id="replyInsert">
		INSERT INTO "TB_COMMENT"
		VALUES(
			SEQ_COMMENT_NO.NEXTVAL, 
			#{commentText}, 
			DEFAULT, 
			DEFAULT, 
			
			<!-- 자식 댓글 --> 
			<if test="parentCommentNo != 0">
				#{parentCommentNo},
			</if>
			
			<!-- 부모 댓글 -->
			<if test="parentCommentNo == 0">
				NULL,
			</if>
			#{memberNo},
			#{boardNo} 
		)
	</insert>

</mapper>
